# Build products
fsh-generated/
output/
temp/
template/
input-cache/

# Tooling deps (restored with `bun install`)
node_modules/

# Local editor and OS files
.DS_Store
.vscode/
.idea/

# NOTE: artifacts produced by `bun run build` ARE committed on purpose so
# build.fhir.org (which only runs the IG Publisher) finds them in place:
#   input/resources/Bundle-period-tracking-longitudinal-example.json
#   input/images/viewer/{app.js,index.html,example.jwe,shl.json,shlink.txt}
