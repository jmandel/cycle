== Suppressed Messages ==

# Reviewed and accepted (see the spec's terminology/mapping guidance):
#
# {score} is the conventional UCUM representation for a dimensionless 0-10
# patient-reported score (matching this guide's mapping contract and the FHIR
# spec's own survey/score examples). The validator's note is a best-practice
# caution, not a defect. Accepted by the IG author.
UCUM Codes that contain human readable annotations like {score} can be misleading (e.g. they are ignored when comparing units). Best Practice is not to depend on annotations in the UCUM code, so this usage should be checked
