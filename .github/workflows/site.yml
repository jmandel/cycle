name: Build and deploy site

# Builds the IG site, generated examples, reference viewers, sample JWE, and
# sample SHLink in the runner. Generated artifacts are uploaded to Pages but are
# not committed.

on:
  push:
    branches: [main]
  schedule:
    # Weekly blank-cache publisher smoke. This does not deploy Pages.
    - cron: '17 9 * * 1'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

env:
  # Exact Rust producer for the closed cycle-site/v2 handoff.
  FIG_COMMIT: 198fb878053582554011ad8e1e9f623ef6e5a675

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'schedule' && 'publisher-safety' || 'pages' }}
  cancel-in-progress: true

jobs:
  publisher-safety:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
      - uses: oven-sh/setup-bun@v2
      - name: Cache Bun install cache
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-
      - name: Cache FHIR packages
        uses: actions/cache@v4
        with:
          path: ~/.fhir/packages
          key: ${{ runner.os }}-fhir-publisher-safety-${{ hashFiles('bun.lock', 'sushi-config.yaml', 'input/fsh/**/*.fsh', 'site-gen/publisher/**/*.ts', 'scripts/check-publisher-*.ts') }}
          restore-keys: |
            ${{ runner.os }}-fhir-publisher-safety-
            ${{ runner.os }}-fhir-
      - name: Install deps
        run: bun install --frozen-lockfile
      - name: Install native build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y graphviz
      - name: Install Jekyll
        run: gem install jekyll --no-document
      - name: Run publisher unit tests
        run: bun run test:publisher
      - name: Run blank-cache publisher smoke
        run: bun run test:publisher:blank-cache
      - name: Run IPS publisher pilot
        run: bun run test:publisher:ips
      - name: Run SDC publisher pilot
        run: bun run test:publisher:sdc
      - name: Run Da Vinci CRD publisher pilot
        run: bun run test:publisher:crd
      - name: Upload publisher diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: publisher-diagnostics
          path: |
            temp/site-gen/**/*.manifest.json
            temp/site-gen/**/*.validation.json
            temp/site-gen/**/*.compare.md
            temp/ips-ig/output/package.db
            temp/ips-ig/output/qa.html
            temp/ips-ig/output/qa.json
            temp/sdc-ig/output/package.db
            temp/sdc-ig/output/qa.html
            temp/sdc-ig/output/qa.json
            temp/crd-ig/output/package.db
            temp/crd-ig/output/qa.html
            temp/crd-ig/output/qa.json
          if-no-files-found: ignore

  build:
    if: github.event_name != 'schedule'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
      - uses: oven-sh/setup-bun@v2
      - name: Cache Bun install cache
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-
      - name: Cache FHIR packages
        uses: actions/cache@v4
        with:
          path: ~/.fhir/packages
          key: ${{ runner.os }}-fhir-${{ hashFiles('bun.lock', 'sushi-config.yaml', 'input/fsh/**/*.fsh', 'site-gen/publisher/**/*.ts') }}
          restore-keys: |
            ${{ runner.os }}-fhir-
      - name: Install native build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y graphviz zip
      - name: Install deps
        run: bun install --frozen-lockfile
      - name: Build pinned Fig producer
        run: |
          FIG_SRC="$RUNNER_TEMP/sushi-rs"
          git init "$FIG_SRC"
          git -C "$FIG_SRC" remote add origin https://github.com/jmandel/sushi-rs.git
          git -C "$FIG_SRC" fetch --depth 1 origin "$FIG_COMMIT"
          git -C "$FIG_SRC" checkout --detach FETCH_HEAD
          cargo build --manifest-path "$FIG_SRC/Cargo.toml" --release -p fig
          echo "FIG_BIN=$FIG_SRC/target/release/fig" >> "$GITHUB_ENV"
      - name: Install Jekyll
        run: gem install jekyll --no-document
      - name: Run publisher unit tests
        run: bun run test:publisher
      - name: Build static site (Cycle v2 closed SiteBuild)
        run: bun run build:sitegen
        env:
          GRAPHVIZ_DOT: /usr/bin/dot
          VIEWER_BASE: https://cycle.fhir.me/view
          PAGES_CNAME: cycle.fhir.me
      - name: Upload publisher diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: publisher-diagnostics-build
          path: |
            temp/site-gen/**/*.manifest.json
            temp/site-gen/**/*.validation.json
            temp/site-gen/**/*.compare.md
            qa.html
            qa.json
          if-no-files-found: ignore
      - uses: actions/configure-pages@v5
      - name: Upload site as Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # site-gen/out is the root static site; Publisher output remains only
          # an independent namespaced QA artifact.
          path: site-gen/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
